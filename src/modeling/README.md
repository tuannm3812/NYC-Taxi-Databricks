# Modeling

Reusable modeling logic should live here as the notebook workflow is productionized.

Recommended responsibilities:

- Split datasets for training, validation, and testing.
- Fit baseline and advanced models.
- Apply leakage-safe encoders and scalers.
- Persist model artifacts and evaluation metrics.
